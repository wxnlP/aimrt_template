aimrt:
  log:
    core_lvl: INFO # Trace/Debug/Info/Warn/Error/Fatal/Off
    backends:
      - type: console
  module:
    pkgs:
      - path: ./librpc_client_pkg.so
        disable_modules: ['PbClientModule']
    modules:
      - name: Ros2ClientModule
        log_lvl: INFO

# Module custom configuration
