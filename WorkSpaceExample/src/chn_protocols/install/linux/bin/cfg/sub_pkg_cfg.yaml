aimrt:
  log:
    core_lvl: INFO # Trace/Debug/Info/Warn/Error/Fatal/Off
    backends:
      - type: console
  module:
    pkgs:
      - path: ./libsubscriber_pkg.so
        disable_modules: ['chn_publisher_module']
    modules:
      - name: ChnSubscriberModule
        log_lvl: INFO

# Module custom configuration
